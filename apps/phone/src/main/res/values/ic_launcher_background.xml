<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The same deep teal for every Seca app: the three icons read as one family,
         and only the glyph tells them apart. A launcher icon is drawn by the system
         before any Compose theme exists, so it cannot follow the palette. -->
    <color name="ic_launcher_background">#FF1F6F74</color>
</resources>
